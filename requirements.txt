# Cliente no oficial de la API de Garmin Connect. Extra "workout" porque
# workout_builder.py usa garminconnect.workout (modelos tipados para construir
# y subir entrenamientos estructurados) — sin esto pydantic solo llegaría de
# rebote vía mcp, una dependencia transitiva de la que no deberíamos depender.
# Ya no se usa el extra "typed": las tools de lectura leen los dicts crudos de
# connectapi, que es contra lo que se verificó campo a campo la especificación
# en docs/LaIA-MCP-metodos-garmin.md (misma versión, 0.3.15).
garminconnect[workout]==0.3.15

# Carga variables desde el fichero .env
python-dotenv==1.2.3

# SDK oficial de Model Context Protocol
mcp==2.1.1

# Servidor HTTP para exponer el MCP como servicio remoto
uvicorn==0.52.4
starlette==1.6.0

# Cliente Postgres async, para el registro de usuarios y el estado OAuth
asyncpg==0.31.0

# Cifrado en reposo del email (con clave) y la sesión de Garmin (ver crypto_utils.py)
cryptography==50.0.1