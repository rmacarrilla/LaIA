# Cliente no oficial de la API de Garmin Connect
garminconnect==0.3.11

# Carga variables desde el fichero .env
python-dotenv==1.2.3

# SDK oficial de Model Context Protocol
mcp==2.1.1

# Servidor HTTP para exponer el MCP como servicio remoto
uvicorn==0.52.4
starlette==1.6.0

# Cliente Postgres async, para el registro de usuarios y el estado OAuth
asyncpg==0.31.0